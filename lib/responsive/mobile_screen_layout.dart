import 'package:flutter/material.dart';

class MobilelScreenLayout extends StatelessWidget {
  const MobilelScreenLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Instagram Clone Mobile ver')),
    );
  }
}
